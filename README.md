# Project_1

This is `Project_1`. The repository contains a Python virtual environment named `venv` and example files.

## Quick start

```powershell
py -3 -m venv venv
.\venv\Scripts\Activate.ps1
python -V
pip install -r requirements.txt  # if you add one
```
